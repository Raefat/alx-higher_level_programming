#!/usr/bin/python3

def uppercase(str):
    upper = ''
    for l in str:
        if ord(l) >= 97 and ord(l) <= 122:
            upper += char(ord(l) - 32)
        elif ord
            upper += l
    print(upper)
